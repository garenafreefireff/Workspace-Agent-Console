# Local Workspace Agent
MCP_WRITE_ENABLED=false
HOST=127.0.0.1
PORT=8787
COMMAND_TIMEOUT_MS=120000
MAX_COMMAND_OUTPUT=120000

# Loop Engineering P1.5 secret references.
# Keep real values only in .env / process environment; never put them in Loop Specs.
ENERGYINSIGHT_EMAIL=datainsight@gmail.com
ENERGYINSIGHT_PASSWORD=123456

# Optional runtime locations.
# LOOP_DATA_DIR=
# LOOP_WORKTREE_DIR=
